# 补交对照 · confucius

负责人：收集人（现网孔子实现）
日期：2026-08-19

上一份 `confucius.zip` 已交 JSON + 入口文件。本包按 02/03 管线补其 `import` 依赖。

| # | 功能 | 本包路径 | 入口 | 备注 |
|---|------|----------|------|------|
| A7 | 选语料依赖 | select_confucius/ | 编排仍是上一份 select_confucius.ts（runConfuciusPipeline） | 02 读库/裁决/粘滞 |
| A9a | 成句依赖 | generate_confucius/ | Prompt 仍是上一份 prompts.ts / sentenceOrganization.ts | 03 分流与口吻护栏 |
| B3 | 提问评价 | （上一份已交） | evaluate_question_confucius.ts | 本包不重交 |
| — | 模型适配 | qwenJson.ts | chatCompletionJson / Text / Stream | 接到工程现有 LLM；不附密钥 |
| — | 表达约束 | utils/confuciusExpression.ts | 放到 src/utils/ | pipeline 从 utils 引用 |

未打入（以通用.zip 为准）：attitudeCompare.ts、outputMode.ts、types.ts、长期 stack 两份、好感、回避句抽取。
未打入（上一份已有）：select_confucius.ts、prompts.ts、sentenceOrganization.ts、evaluate_question_confucius.ts、全部 JSON。
未打入：path2Stretch.ts（未接入）、gloss.ts（入口未引用）、constants/app.ts、apiFetch.ts、产品壳。
